language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-grunt-config' ]; then cd .. && eval "mv $currentfolder generator-grunt-config" && cd generator-grunt-config; fi

